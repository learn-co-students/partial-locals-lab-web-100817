Classroom Info
<%= render 'classroom', classroom: @classroom %>

We now want to call out the oldest student in the class:
<% @classroom.students.each do |s| %>
  <%= render 'students/student', student: s %>
<% end %>
